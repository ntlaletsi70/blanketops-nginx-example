BlanketOps NGINX Example Test File

Dummy file to persue trivial commits for testing, and notes
AWS_REGION value causing authentication issue, troubleshooting, this required for Deployment to ECS
Some problems
Trigger New

kubectl port-forward --namespace kourier-system $(kubectl get pod -n kourier-system -l "app=3scale-kourier-gateway" --output=jsonpath="{.items[0].metadata.name}") 8080:8080 19000:9000 8443:8443
curl -v -H "Host: github-message-dumper.default.127.0.0.1.nip.io" http://localhost:8081

